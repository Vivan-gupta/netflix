function playMovie(videoSource) {
  // You can implement the logic to play the selected movie here
  alert(`Playing: ${videoSource}`);
  // For a real implementation, you would use a video player library or HTML5 video element.
}
